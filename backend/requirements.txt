# ==========================================
# backend/requirements.txt
# ==========================================

# Web framework
fastapi
uvicorn[standard]
python-multipart     # Multipart/form-data parsing for UploadFile (voice transcribe endpoint)

# LLM
groq

# Environment / config
python-dotenv
pydantic-settings

# Database
sqlalchemy
psycopg2-binary      # Neon Postgres driver
alembic              # DB migrations

# Security / validation
sqlparse             # SQL guard AST parsing
slowapi              # Rate limiting
email-validator      # Required by Pydantic for EmailStr

# Auth (Phase 2)
bcrypt               # Password hashing (passlib is abandoned & incompatible with bcrypt>=4)
python-jose[cryptography]   # JWT

# Testing (Phase 5)
pytest
httpx                # FastAPI async test client + Deepgram REST calls (finance/stt.py)
